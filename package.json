{
  "private": true,
  "name": "airbnb_clone",
  "scripts": {
    "build:server": "lerna run build --scope={@abb_clone/common,@abb_clone/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.17.0"
  }
}
